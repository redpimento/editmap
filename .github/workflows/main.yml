name: Sitemap Updater

on:
  schedule:
    # 매일 오전 6시 00분 (KST)에 실행 (UTC 기준 21시)
    - cron: '0 21 * * *' 
  workflow_dispatch:
    # 수동 실행을 허용합니다.

jobs:
  update_files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # 커밋을 위해 전체 히스토리가 필요합니다.
          fetch-depth: 0 
          # 🚨 수정된 부분: checkout 단계에 GH_TOKEN을 전달하여 푸시 권한을 부여합니다.
          token: ${{ secrets.GH_TOKEN }} 
          
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          
      - name: Install dependencies
        run: pip install requests
        
      - name: Execute Python script to fetch and fix files
        run: python sitemap_fixer.py
        
      - name: Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Automated update: Fixed sitemap and rss files"
          commit_user_name: 'redpimento' 
          commit_user_email: 'redpimento@users.noreply.github.com'
          push_options: '--force'
          # 🚨 수정된 부분: 오류를 일으켰던 'token' 입력줄을 삭제했습니다.